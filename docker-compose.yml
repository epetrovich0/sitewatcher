version: "3.9"

services:
  backend:
    build: ./backend
    container_name: sitewatcher-backend
    restart: unless-stopped
    env_file: ./backend/.env
    volumes:
      - backend_data:/app
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  frontend:
    build: ./frontend
    container_name: sitewatcher-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  backend_data:
